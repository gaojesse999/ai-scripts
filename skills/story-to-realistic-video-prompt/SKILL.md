---
name: story-to-realistic-video-prompt
description: 把用户的一句话点子或一小段剧情，扩写成一条生动的中文真人电影感视频生成提示词，风格贴近 IMAX 真人实拍（非动漫、非卡通）。默认按 15 秒单镜片段产出。按七模块分层结构组织：全局规格→基调情绪→场景环境→主体锚点→运镜方案→动作时间线→收尾定格。强调“一镜一核心动作、运镜全局解耦+节拍内镜动同句绑定、出场即定义、删元指令、去重复形容词”，保留极致堆叠密度。内置挂载前缀、项目圣经优先级、人物/场景/语音一致性与音频硬约束方案。默认输出 1500-1800 中文字符。适用于：剧情扩写视频提示词、15s视频提示词、短点子扩写、真人电影感提示词、Sora/Veo/可灵/即梦/Runway 文生视频与图生视频、动作戏/对话戏/氛围戏分镜提示词。
argument-hint: '一句话点子或一小段剧情，可选 @场景/@人物 与【全局约束】'
---

# 剧情转真人电影感视频提示词

## 核心目标

输入：用户的一句话点子，或一小段剧情/分镜文字。
输出：一条**生动、可直接用于视频模型**的中文长提示词，风格贴近真人实拍电影（IMAX 质感），保留**极致堆叠密度**，但通过去重复形容词与控节拍数做到密而不啰嗦。

**默认以 15 秒单镜片段为目标**，除非用户特别说明其它时长。15 秒片段默认写 1500-1800 中文字符（不少于 1500、不超过 1800）；时长越长，按比例增加节拍与字数，但每节拍仍只承载一个核心动作。

只输出最终提示词正文，不解释、不写大纲、不用 Markdown、不显示分析过程。
若用户要多个版本，用 `版本1`、`版本2` 标签，每个都是完整独立提示词。
若用户要拆分长脚本，按 15 秒为单位分段，每段都按本结构独立成稿，保持密度且不超长。

## 何时使用

- 用户给一句话/一小段剧情，想要可直接投喂模型的视频提示词。
- 需要真人电影质感（非动漫、非卡通、非插画）的镜头。
- 需要把抽象剧情落成"可成像"的画面、运镜与物理反馈。

## 写作前静默提取（不向用户复述）

- **主体**：出场角色/怪物/关键道具的名称与当前状态。
- **地点**：时间、天气、光线、空间、地面材质、氛围元素。
- **事件节拍**：先发生什么→发生什么变化→谁反应→如何收尾。
- **情绪**：紧张/压迫/傲慢/恐怖/庄严/悲伤/幽默。
- **场景类型**：动作 / 叙事 / 对话 / 混合（决定运镜与节奏）。
- **全局约束**：用户给的任何【方括号要求】原样保留，放在开头附近。
- **挂载引用**：用户写的 `@场景/@人物`，或文本中可识别的核心对象，整理成挂载前缀。

仅当"缺主体"或"缺地点"且无法合理推断时，才向用户提问；否则直接扩写。

## 工程化：挂载前缀与项目圣经优先级

为保证跨镜头、跨生成的一致性，沿用成熟流水线的挂载与圣经机制：

- **自动挂载前缀**：最终提示词必须以 `@名称` 前缀开头，把文本中的主要对象整理为固定顺序：主场景 → 主要人物 → 怪物/敌人 → 关键道具/语音/设定。用户已写 `@` 则保留并补齐遗漏；未写则自动生成。说话角色的语音也进前缀，如 `@女巫语音`。别名归并为正式名称（如 `法师`→`女巫`）。
- **项目圣经（若工作区存在则生成前默认读取，不写进正文/前缀）**：
  1. `assets/整体风格.md`：最高优先级的视觉、光照、声音、镜头、技术风格来源。
  2. `assets/人物圣经.md`：人物身份、关系、说话方式来源；外貌以挂载图片为准，不重述。
  3. `assets/场景圣经.md`：空间结构、动线、道具、光源来源。
- **优先级顺序**：用户最新明确要求 > 挂载图片/音频/场景素材 > `整体风格.md` > `人物圣经.md` > `场景圣经.md` > 本 skill 默认规则。只把这些原则落实到提示词，不整段复述文件内容。

## 音频与一致性硬约束

当用户提到"挂载人物/场景/语音""使用参考角色/场景""不要生成新声音""直接使用语音"等要求时，把约束放在提示词最前部（挂载前缀之后）。默认合并写法：

`【挂载资源与音频硬约束】严格使用已挂载的人物形象、角色设定、场景参考、怪物参考和语音素材；人物外貌、服装、发型、气质、阵营风格、场景空间结构、建筑造型、怪物形态不得偏离挂载素材。已有挂载人物图片或人物圣经时，不重新发明、不重新描述外貌服装发型，只写"严格参考挂载形象"。无字幕、无屏幕文字、无背景音乐、不生成配乐。语音两种模式：默认模式以挂载语音为音色参考，由模型重新生成对应台词，但音色、语气、节奏、停顿贴近参考；直接使用语音模式原样使用挂载语音文件，不重新生成、不改音色节奏、不加新旁白。无论哪种模式，都必须生成真实环境音效与动作音效（风声、脚步、火焰、布料摩擦、武器碰撞、怪物低吼、毒雾翻涌等符合场景的声音）。`

- **语音模式判断**：仅说"挂载/参考语音"→ 按音色参考；明确说"直接使用提供的语音/不要重新生成声音/使用原始语音文件"→ 按直接使用语音。
- **一致性兜底**：指定具体说话人或地点时，写清"说话人物必须是该挂载形象；场景必须使用该挂载地点的空间结构"，不得替换。
- 用户未提供挂载时，一致性退化为文字锚点（见模块 5），并通过固定外观锚点 + 建议复用同一参考图/种子来保持跨镜一致。

## 挂载素材与提示词内容核对（强制）

当主体、环境、道具是以图片/参考素材挂载进来时，生成前必须先核对提示词里的动作、姿态、道具使用是否与挂载素材中的事实**物理自洽**，避免写出与画面矛盾的动作。核对维度：

- **手部/肢体占用**：先确认挂载形象的双手与肢体当前被什么占用。例如左右手都已持武器/法杖/盾牌，就不能再写"用手挠头、捋头发、掏口袋、双手抱胸、徒手格挡"等需要空手的动作；要用手做新动作，必须先写明合理的"收/换/抛/插回"过渡（如"先将短剑插回腰侧"），再做下一动作。
- **道具状态**：挂载道具的形态、位置、破损状态要延续。已挂载"裂开的法杖"不能凭空写成完好；已背在身后的武器不能在没有拔出动作时直接挥砍。
- **服装/装备约束**：挂载的服装、护甲、披风会限制动作幅度与可见部位。重甲不写轻盈翻滚，蒙面不写"嘴角微扬"这类不可见表情，长袍束缚不写大跨度劈叉。
- **身姿/朝向起点**：以挂载图的姿态、重心、朝向为动作起点，不让角色凭空瞬移、突然站直或获得未铺垫的新姿态。
- **环境空间约束**：挂载场景的空间结构、出入口、地形、光源方向决定走位与视线。不写穿墙、不在挂载封闭空间里凭空出现新通道、不让光影方向与挂载光源冲突。
- **数量与身份**：挂载人物/怪物的数量、阵营、关系要一致，不无故增减主要角色，不让挂载道具在同一镜头内重复出现两份。

处理方式：发现提示词意图与挂载事实冲突时，**优先改写动作以适配挂载素材**（加过渡动作或换一个不冲突的动作），而不是改变挂载形象本身；只有用户明确要求改变形象时才偏离挂载。核对过程静默执行，不写进正文。

## 挂载即省略：有挂载就不再描述形象（强制）

只要某主体或场景已经在挂载前缀中以 `@名称` 引用、或出现在项目圣经中，就**严禁在提示词正文里复述其外貌、服装、发型、配饰、肤色、瞳色、身材比例、建筑造型、空间布局、地形植被等"形象类"信息**。模型已通过挂载图获得这些事实，再写一遍只会引入冲突或浪费字数预算。

**只允许写、且必须写的，是动作所需的最小信息**：

- **位置/朝向起点**："走在最前/居中略后/殿后"、"面向门口"、"背靠石墙"——给动作一个空间起点。
- **手部与道具占用状态**："双手各握一把手斧垂于身侧"、"右手持法杖、左手空闲"——为后续动作的手部使用建立约束。
- **当前动态状态**：行进中/停步/转身/蹲伏——是动作的起始姿态而非外观。
- **环境的光照方向与天气状态**：挂载场景图本身的光线方向、雾气、积水等可以作为动作物理反馈的依据被提及（如"积水被靴底踩出涟漪"），但**不重述空间结构、建筑细节、植被造型**。

**对挂载主体只写一句**：`严格参考挂载形象`（或 `严格参考挂载形态`），后接其在本镜头中的位置/朝向/手部占用，其它一律省略。

**何时仍需写形象描述**：

- **未挂载的角色/场景/道具**：用 3-5 个最具辨识度的外观锚点（出场即定义）。
- **挂载基础上派生的多个个体**（如挂载了"沉沦魔"形态，本镜头出现 4 只）：先一句"严格参考挂载形态"作为共性基准，再**只写每只之间的差异点**（角形/伤疤/装备磨损/体型差异等），不重述共性。
- **环境光线/天气随剧情变化**：用户要求"清晨/黄昏/暴雨"等不同于挂载图基准光线的氛围时，可写新的光线状态，但不重述空间结构。

**自检问句**（生成后静默通读一遍）：每一句外貌/环境形象描写，是否在挂载图里已经能看到？如果是，删掉。

## 七模块输出结构

写成**一段流动的中文长段落**（不分点、不加小标题），但内部按以下顺序组织内容：

1. **挂载前缀 + 全局约束**：`@主场景，@主要人物，@怪物，@关键道具`，随后原样附上用户的【方括号约束】（若有）。
2. **全局规格（1 句）**：画质 + 媒介/机型 + 渲染 + 排除项。例：`8K IMAX 院线真人实拍质感，全画幅原生拍摄，PBR 物理材质渲染，真人实拍物理逻辑，无虚拟CG、无廉价平面特效、无字幕、无背景音乐。`
3. **基调情绪（半句到一句）**：题材 + 情绪关键词 + 时长意图。
4. **场景环境**：**有挂载场景**时只写本镜头需要的"动态/光线状态"（清晨/黄昏/暴雨/夜风/雾气流动/积水涟漪等可随剧情改变的氛围状态），**绝不复述空间结构、建筑造型、植被布局、地形分布**（这些由挂载图固定）；**无挂载场景**时再补全光线方向、空气流动、地面材质、1-2 个标志性环境细节，落在"可见物理"上。
5. **主体锚点**：每个出场对象"出场即定义"——**有挂载参考则只写"严格参考挂载形象"+本镜头的位置/朝向/手部占用一行，不复述任何外貌服装发型**；同一挂载形态衍生的多个个体只写**个体之间的差异点**；**无挂载参考**才给 3-5 个最具辨识度的外观锚点。详见"挂载即省略"章节。
6. **运镜方案（全局总纲先交代）**：机位 + 运动方式 + 景别 + 节奏，作为整段的全局运动地图。这里**只解耦描述位置**——先建立"镜头整体怎么动、几次主要运动、景别走向"，不在此处展开角色动作。
7. **动作时间线 + 收尾**：按节拍推进，**每个节拍内把镜头行为与角色动作写在同一句、按同一时刻绑定**（运镜变化 + 核心动作 + 物理反馈 + 微表情），以带压力的最终画面/特写/定格收尾。

## 运镜与动作如何保持一致（关键）

"运镜与动作解耦"指的是**描述层级**，不是时间上分家：

- **全局总纲（模块 6）= 地图**：先统一镜头逻辑，避免镜头语言散落各处互相矛盾（如声明一镜到底却又写切镜）。
- **节拍绑定（模块 7）= 逐帧导航**：一致性在这里实现。每个节拍必须写成 `[运镜变化] + [核心动作] + [物理反馈] + [微表情]`，镜头与动作在**同一节拍、同一时刻**出现，模型不会理解成两件无关的事。
- 例：`镜头从肩后顺势抬升成正面低角度仰视近景——女巫缓缓抬头，冷月光切过眉骨，瞳孔轻微收缩`（抬升运镜与抬头动作锁在一个节拍）。
- 总纲管"全局不冲突"，节拍管"局部同步"。绝不允许出现"先列一段纯运镜、再列一段纯动作"的分家写法。

## 五条铁律

1. **一镜一核心动作**：单个节拍只承载 1 个主动作；动作多就拆成多个节拍（15s 通常 2-4 个），不要在一句里塞 8 个连续动作。
2. **运镜全局解耦、节拍内镜动同句绑定**：模块 6 只写全局运动总纲；模块 7 每个节拍内镜头与动作必须同句出现、同步对应。
3. **出场即定义、挂载即省略**：任何专有名词/角色第一次出现都要被定义——**有挂载就只写"参考挂载"+位置/手部状态，绝不重述外貌**；**无挂载才给外观锚点**。挂载素材中已确定的手部占用、道具状态、服装约束，动作不得与之冲突（如双手持械时不写挠头）。
4. **删元指令**：删除只对人类有意义、不可成像的话——如"强化氛围""100%还原您的标准""拉满""务必"等工序备注，不写进正文。
5. **去重复形容词（保留极致密度）**：密度照旧极致堆叠可见细节，但**同一含义的形容性描述只保留一个最准的**——"极致写实/百分百还原/1:1/真实可感"这类同义强调词只取其一，绝不并列堆叠。

## 密度与字数控制

- **保留极致密度（限定在动作/物理/运镜/表情维度）**：每个节拍仍要堆叠**动作的物理反馈、动作触发的动态材质（雨打皮肤/泥水飞溅/火星迸射/布料被风掀动等）、面部微表情、镜头连续性**，读起来像剧情梗概就继续补这些可见细节。极致密度**不延伸到挂载主体的静态外貌/服化道**——那些一律省略。
- **唯一的删减目标是重复**：同义形容词叠加（"极致写实/百分百还原/1:1"同时出现）→ 只留一个；同一材质/同一质感词反复强调 → 只在最相关处出现一次；与动作无关的超长静态服化道清单 → 压缩为 3-5 个最具辨识度锚点。删的是重复，不是细节。
- **挂载主体/场景一律省略形象描述**：已在挂载前缀中引用的人物、怪物、场景、道具，正文中**严禁出现外貌、服装、发型、建筑、空间结构等形象类描写**，只保留"参考挂载 + 动作所需位置/手部状态/光线动态"。这是省字数和避免冲突的最大单一来源。
- **保留标准**：每一句都应让画面**新增可见信息**（新动作、新物理反馈、新空间关系、新表情）。新增信息的句子一律保留，哪怕密集。
- **字数**：默认 15s → **1500-1800 中文字符**（硬性区间，低于 1500 视为信息密度不足，高于 1800 视为冗余啰嗦）；更长时长按节拍比例放大。把字数预算优先花在动作物理因果、运镜空间交代、关键表情、动作触发的环境物理反馈，而不是重复"真人实拍质感"或复述挂载形象。

## 场景类型 → 运镜/节奏路由

- **动作类**（追逐/对决/冲击）：节奏"蓄势慢→命中快→余波慢"；冲击点为画面中心；连续动作可一镜到底或快切，每节拍聚焦一个攻击主体。
- **叙事类**（旅程/氛围/揭示）：旅程用跟拍/并行；氛围用静态或缓慢推进、靠雨雾火风承载；揭示用横摇/推轨控制观众何时看见。
- **对话类**（对抗/审讯/谈判）：对抗用过肩+反应特写表现权力转移；审讯用低角度逼问+推进特写；谈判用对称构图、克制运动。
- **混合类**：类型切换要有可见过渡，动作爆发要由前面的发现/台词/冲突推动。

输出只体现结果，不展示判断过程。

## 连续性与一致性自检（静默执行）

- 空间：谁在正/背/高/低/远，攻击路径与视线方向是否合理。
- 物理：抛出的物体会落下，发丝继续随风，听到/看到之后才反应；受击反馈、武器/法术轨迹前后一致，无瞬移、无凭空新姿态。
- 因果：高潮由前面动作自然推动，不是突兀出现。
- **角色一致性**：有挂载/圣经时严格参考，跨节拍不改变外貌服装发型；**正文不重复描述挂载已涵盖的形象信息**；无挂载时以首次出场的外观锚点为准，后续节拍不凭空变装。
- **挂载事实核对**：主体/环境/道具为挂载时，逐条核对动作与挂载事实是否物理自洽（手部占用、道具状态、服装约束、身姿朝向、空间结构、数量身份），冲突则改写动作适配素材（见上文"挂载素材与提示词内容核对"）。
- **多人/群像（分挂载情况处理）**：① 若群像**未挂载**或为新增无名角色，避免同脸复制——同阵营人物保持统一风格，但脸型、五官比例、肤色、发型、身高体态与护甲/披风/武器/配饰/磨损痕迹要有细微差异。② 若群像为**同一挂载形态衍生的多个个体**（如挂载"沉沦魔"形态、本镜头出现 4 只），不重述共性形态，**只写个体之间的差异点**（伤疤位置、装备磨损、武器变体、体型偏差、角形/鬃毛差异等），脸型/肤色/瞳色等共性特征已由挂载承担，不再描述。
- **回忆/转场**：用户要求"回忆/曾经/传说"时，可从说话人面部近景推进到双眼，瞳孔倒影（火光/月光/环境）拉长扭曲过渡为回忆场景的对应光源；要求"一直在回忆里"则不再切回现实。

## 极简输入扩写规则

用户只给一句话时：

- 保留命名角色与必需动作。
- 补全场景物理（动态材质反馈，不复述挂载场景的静态结构）、运镜总纲、每个节拍的镜动同句绑定、关键表情锚点、反应节拍。
- 最多新增 1 个道具（除非用户要求更多）。
- 默认按 15s 组织 2-4 个主镜头节拍，保持画面"可渲染、可执行"，不塞复杂调度。

## 负面约束

不要出现：动漫/二次元/卡通/插画感（除非用户要求）；塑料感/AI感（除非作为负面词）；没有身体表现的抽象情绪；同节拍矛盾的光线或镜头方向；过多无名新增角色；以及第 4 条所禁的元指令。

## 输出心智模板（不显示占位符）

`@主场景，@人物，@怪物，@道具。【用户全局约束（若有）】。【挂载资源与音频硬约束（若有挂载/语音）】。[全局规格一句]。[基调情绪半句]。[环境：有挂载场景→只写本镜头光线/天气动态；无挂载→补全空间细节]。[主体锚点：有挂载→"严格参考挂载形象"+位置/手部占用一行；无挂载→3-5个外观锚点；同形态多个体→只写差异点]。开场，[运镜总纲：机位+运动+景别]，[节拍1：运镜变化同句绑定核心动作+物理反馈+微表情]。紧接着，[镜头变化重新交代空间，节拍2：另一主体的反应动作+材质细节]。[节拍3：压力升级动作或一句短对白]。最终画面，[特写/反应/环境静止]，画面定格收尾。`

## 微型示例（输入 → 输出方向）

输入：`女主在森林树上嘊瓜子，下面两人发现她，她威胁他们交出船。`

输出方向（默认 15s、1500-1800 字的一段式长提示词）：自动挂载前缀 → 全局真人实拍规格一句 → 懒散傲慢基调 → **环境块按挂载情况二选一**：若 `@森林` 已挂载则只写本镜头的光线动态（晨雾流动/光斑跳动/落叶飘飞）而不再描述树木/植被造型；若未挂载则补全树冠枝叶/丁达尔光束等空间细节 → **主体锚点同理**：女主若已挂载只写"严格参考挂载形象，盘坐在高处树枝上，单手嘊瓜子"，**不再描述外貌服装**；未挂载才给 3-5 个外观锚点 → 低角度仰视揭示女主嘊瓜子（节拍1：运镜与瓜子壳轻落+手指动作+微表情同句）→ 镜头环绕下移交代地面二人位置同时二人抬头发现的反应（节拍2）→ 女主轻蔑短句威胁"船，留下"（节拍3，嘴型+眼神）→ 最终紧张面部特写定格。全程一镜一动作、运镜总纲先行且节拍内镜动同句绑定、同义形容词只留一个、挂载主体不重述形象，控制在 1500-1800 字。
