# Floating Point → Float Division

*Auto-generated from CEVA-XC4500 Vec-C Intrinsics documentation*

---

Main table → Floating Point → Float Division

Float Division

Instruction Type and Width                      Instruction description
                                                Performs a reciprocal action on a 32-bit single precision
                                                floating-point source. The sources and the destination are either
Vector Floating Point Division                  32-bit or 40-bit wide determined by the vector register type. This
                                                is a non-native instruction and is implemented as software
                                                sequence.
                                                Performs a reciprocal action on a 32-bit single precision
                                                floating-point source, with Newton-Raphson algorithm which
                                                extends the mantissa’s precision. The sources and the
Vector Floating Point Division (4 Operations)
                                                destination are either 32-bit or 40-bit wide determined by the
                                                vector register type. This is a non-native instruction and is
                                                implemented as software sequence.
                                                Performs a reciprocal sqaure-root action on a 32-bit single

precision floating-point source, with Newton-Raphson algorithm
                                                which extends the mantissa’s precision. The sources and the
Vector Floating Point Division (N-R)
                                                destination are either 32-bit or 40-bit wide determined by the
                                                vector register type. This is a non-native instruction and is
                                                implemented as software sequence.
                                                Performs a reciprocal sqaure-root action on a 32-bit single
                                                precision floating-point source, with Newton-Raphson algorithm
Vector Floating Point Division (N-R, 4          which extends the mantissa’s precision. The sources and the
Operations)                                     destination are either 32-bit or 40-bit wide determined by the
                                                vector register type. This is a non-native instruction and is
                                                implemented as software sequence.
                                                Performs a reciprocal sqaure-root action on a 32-bit single
                                                precision floating-point source. The sources and the destination
Vector Floating Point Reciprocal SQRT           are either 32-bit or 40-bit wide determined by the vector register
                                                type. This is a non-native instruction and is implemented as
                                                software sequence.
                                                Performs a reciprocal sqaure-root action on a 32-bit single
                                                precision floating-point source. The sources and the destination
Vector Floating Point Reciprocal SQRT (4
                                                are either 32-bit or 40-bit wide determined by the vector register
Operations)
                                                type. This is a non-native instruction and is implemented as
                                                software sequence.
                                                Performs a reciprocal sqaure-root action on a 32-bit single
                                                precision floating-point source, with Newton-Raphson algorithm

which extends the mantissa’s precision. The sources and the
Vector Floating Point Reciprocal SQRT (N-R)
                                                destination are either 32-bit or 40-bit wide determined by the
                                                vector register type. This is a non-native instruction and is
                                                implemented as software sequence.
                                                Performs a reciprocal sqaure-root action on a 32-bit single
                                                precision floating-point source, with Newton-Raphson algorithm
Vector Floating Point Reciprocal SQRT (N-R,     which extends the mantissa’s precision. The sources and the
4 Operations)                                   destination are either 32-bit or 40-bit wide determined by the
                                                vector register type. This is a non-native instruction and is
                                                implemented as software sequence.
                                                Performs a sqaure-root action on a 32-bit single precision
                                                floating-point source. The sources and the destination are either
Vector Floating Point SQRT
                                                32-bit or 40-bit wide determined by the vector register type. This
                                                is a non-native instruction and is implemented as software
                                            sequence.
                                            Performs a sqaure-root action on a 32-bit single precision
                                            floating-point source. The sources and the destination are either
Vector Floating Point SQRT (4 Operations)   32-bit or 40-bit wide determined by the vector register type. This
                                            is a non-native instruction and is implemented as software
                                            sequence.
                                            Performs a sqaure-root action on a 32-bit single precision
                                            floating-point source, with Newton-Raphson algorithm which
                                            extends the mantissa’s precision. The sources and the
Vector Floating Point SQRT (N-R)
                                            destination are either 32-bit or 40-bit wide determined by the

vector register type. This is a non-native instruction and is
                                            implemented as software sequence.
                                            Performs a sqaure-root action on a 32-bit single precision
                                            floating-point source, with Newton-Raphson algorithm which
Vector Floating Point SQRT (N-R, 4          extends the mantissa’s precision. The sources and the
Operations)                                 destination are either 32-bit or 40-bit wide determined by the
                                            vector register type. This is a non-native instruction and is
                                            implemented as software sequence.
