# Move And Pack → Move

*Auto-generated from CEVA-XC4500 Vec-C Intrinsics documentation*

---

Main table → Move And Pack → Move

Move

Instruction Type and Width     Instruction description
                               Performs data move from source to destination. The source is a
Vector Move Accumulator
                               GCU 32-bit accumulator. The destination is 32-bit wide.
                               Performs data move of an immediate value to destination. The
Vector Move Immediate          destination is either 32-bit or 40-bit wide determined by the
                               vector register type.

Performs data move from source to destination. The source is
Vector Move VB                 32-bit wide and destination is either 32-bit or 40-bit wide
                               determined by the vector register type.
                               Performs two data move from source to destination. The
                               sources are 16-bit wide and the destinations are either 16-bit or
Vector Move VB Word Parts
                               20-bit wide packed into 32-bit or 40-bit wide determined by the
                               vector register type.
                               Performs data move from source to destination. The source is
Vector Move VM                 40-bit wide and destination is either 32-bit or 40-bit wide
                               determined by the vector register type.
                               Performs two data move from source to destination. The
                               sources are 20-bit wide and the destiantion are either 16-bit or
Vector Move VM Word Parts
                               20-bit wide packed into 32-bit or 40-bit wide determined by the
                               vector register type.
