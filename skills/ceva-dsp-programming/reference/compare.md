# Comparison And Predicates → Compare

*Auto-generated from CEVA-XC4500 Vec-C Intrinsics documentation*

---

Main table → Comparison And Predicates → Compare

Compare

Instruction Type and Width          Instruction description
                                    Performs comparison between two sources and set vector
Vector Compare                      predicate bits accordingly. The sources are 32-bit wide. The
                                    destination is 8-bit VPREX.
                                    Performs comparison between two sources and sets vector
Vector Compare 20-bits              predicate registers accordingly. The sources are 20-bit wide. The
                                    destination is 16-bit VPREX.
                                    Performs comparison between two complex number sources
Vector Compare Complex              and set vector predicate bits accordingly. The sources are 32-bit
                                    wide. The destination is 8-bit VPREX.
                                    Performs comparison between two long number sources and set
Vector Compare Complex Long         vector predicate bits accordingly. The sources are 64-bit wide.
                                    The destination is 8-bit VPREX.
                                    Performs comparison between two sources and sets vector
Vector Compare Complex Word Parts   predicate registers accordingly. The sources are 16-bit wide. The
                                    destination is 16-bit VPREX.
                                    Performs comparison between two long number sources and set
Vector Compare Long                 vector predicate bits accordingly. The sources are 64-bit wide.

The destination is 4-bit VPREX.
                                    Performs comparison between two sources and sets vector
Vector Compare Word Parts           predicate registers accordingly. The sources are 16-bit wide. The
                                    destination is 16-bit VPREX.
