# Arithmetic → Subtract

*Auto-generated from CEVA-XC4500 Vec-C Intrinsics documentation*

---

Main table → Arithmetic → Subtract

Subtract

Instruction Type and Width          Instruction description
                                    Performs two subtractions. Each subtraction is between two
                                    sources into destination. The sources are 16-bit, the destination
Vector Subtract 16-bit Word Parts
                                    are either 16-bit or 20-bit and packed into either 32-bit or 40-bit
                                    destination determined by the vector register type.
                                    Performs two subtractions. Each subtraction is between two
                                    sources into destination. The first source is either 16-bit or 20-
Vector Subtract 20-bit Word Parts   bit (‘out’ switch) and the second source is 20-bit wide. The
                                    destination is 20-bit and packed into 40-bit destination vector
                                    register type.
                                    Performs subtraction between two sources into a destination.
Vector Subtract 32-bit              The sources and the destination are either 32-bit or 40-bit wide
                                    determined by the vector register type.
                                    Performs subtraction between two sources into a destination.
Vector Subtract 40-bit              The first source is 32-bit or 40-bit wide (‘out’ switch) and the
                                    second source is 40-bit wide. The destination is 40-bit wide.
                                    Performs subtraction between two sources into destinations.
Vector Subtract 64-bit              The sources and the destinations are 64-bit or 72-bits wide
                                    determined by the vector register type.
                                    Performs subtraction between two sources into destinations.
                                    The first source is 64-bit or 72-bits wide (‘out’ switch) and the
Vector Subtract 72-bit
                                    second source is 72-bit wide. The destination is 72-bit vector
                                    register type.
