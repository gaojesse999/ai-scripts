# Multiplication → Absolute Square

*Auto-generated from CEVA-XC4500 Vec-C Intrinsics documentation*

---

Main table → Multiplication → Absolute Square

Absolute Square

Instruction Type and Width                Instruction description
                                          Performs squaring of complex absolute. Each complex source is
Vector Complex Absolute Square            32-bits wide (16-bit real and 16-bit imaginary). The products are
                                          written to 40-bit destinations.
                                          Performs squaring of complex absolute. Each complex source is
                                          64-bits wide (32-bit real and 32-bit imaginary). The products are
Vector Complex Absolute Square Long
                                          written to 64-bit or 72-bit destinations determined by the vector
                                          register type.
                                          Performs two operations of squaring of complex absolute. Each
Vector Complex Absolute Square and Pack   complex source is 32-bits wide (16-bit real and 16-bit imaginary).
                                          The products are packed and written to 40-bit destinations.
                                          Performs two operations of squaring of complex absolute. Each
Vector Complex Absolute Square and Pack   complex source is 32-bits wide (16-bit real and 16-bit imaginary).
with Exponent                             The third source is the exponent. The products are packed and
                                          written to 40-bit destinations.
                                          Performs complex absolute squaring. Each complex source is
Vector Complex Absolute Square with       32-bits wide (16-bit real and 16-bit imaginary). The second
Exponent                                  source is the exponent. The products are written to the 40-bit

destination.
