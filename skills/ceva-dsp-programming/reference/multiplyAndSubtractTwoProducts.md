# Multiplication → Multiply and Subtract Two Products

*Auto-generated from CEVA-XC4500 Vec-C Intrinsics documentation*

---

Main table → Multiplication → Multiply and Subtract Two Products

Multiply and Subtract Two Products

Instruction Type and Width                     Instruction description
                                               Performs multiplication and subtraction between two products.
16x16 Multiply and Subtract Two Products       Each source is 16-bit wide. The result is placed into 40-bit
                                               destination.
                                               Performs multiply-addition-subtract using two products. Each
16x16 Multiply, Subtract using Two Products    source is 16-bit wide. The first product is added to the 40-bit
and Accumulate                                 destination and the second product is subtracted from the 40-bit
                                               destination.
                                               Performs two multiplication operations between two sources
16x8 Multiply and Subtract Two Products into   and a constant, and subtraction between the two products. The
Two Words                                      sources are 16-bit wide each and a constant of 8-bit. The 20-bit
                                               results are packed into a 40-bit destination.
                                               Performs two multiply-subtract and accumulate operations. The
                                               sources are 16-bit each and a constant of 8-bit. The first product
16x8 Multiply and Subtract using Two
                                               is added and the second product is subtracted from the 20-bit
Products and Accumulate into Two Words

destination. Both 20-bit results are packed into 40-bit
                                               destination.
                                               Performs multiplication and subtraction between two products.
32x32 Multiply and Subtract Two Products       Each source is 32-bit wide. The result is placed into 72-bit
                                               destination.
