# Arithmetic → Addition

*Auto-generated from CEVA-XC4500 Vec-C Intrinsics documentation*

---

Main table → Arithmetic → Addition

Addition

Instruction Type and Width       Instruction description
                                 Performs two additions. Each addition is between two sources
                                 into destination. The sources are 16-bit and the destinations are
Vector Add 16-bit Word Parts
                                 either 16-bit or 20-bit wide and are packed into either 32-bit or
                                 40-bit destination determined by the vector register type.
                                 Performs two additions. Each addition is between two sources
                                 into destination. The first source is either 16-bit or 20-bit wide
Vector Add 20-bit Word Parts     (‘out’ switch) and the second source is 20-bit wide. the
                                 destination is 20-bits and packed into 40-bit destination vector
                                 register type.
                                 Performs addition between two sources into destination. The
Vector Add 32-bit                sources are 32-bit and the destinations are either 32-bit or 40-bit
                                 wide determined by the vector register type.
                                 Performs addition between two sources into destination. The

Vector Add 40-bit                first source is 32-bit or 40-bit wide (‘out’ switch) and the second
                                 source is 40-bit wide. The destinations are 40-bit wide.
                                 Performs addition between two sources into destinations. The
Vector Add 64-bit                sources are 64-bit vector register type and the destination is 72-
                                 bit vector register type.
                                 Performs addition between two sources into destinations. The
                                 first source is 64-bit or 72-bits wide (‘out’ switch) and the second
Vector Add 72-bit
                                 source is 72-bit wide.The destination is 72-bit vector register
                                 type.
